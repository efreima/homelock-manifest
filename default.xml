<manifest>
    <!-- Origins that get referenced when pulling in code via repo sync -->
	<remote name="github-origin" fetch="https://github.com/efreima/" />
	<remote name="poky-origin" fetch="https://git.yoctoproject.org/git/" />

    <!-- Remote Poky repo -->
	<project name="poky" remote="poky-origin" revision="honister" >
	</project>

    <!-- Remote hLOS repo -->
	<project name="homeLock" remote="github-origin" revision="integration" >
	</project>


</manifest>
                   
