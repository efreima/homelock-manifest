<manifest>
    <!-- Origins that get referenced when pulling in code via repo sync -->
	<remote name="github-origin" fetch="https://github.com/efreima/" />
	<remote name="yocto-origin" fetch="https://git.yoctoproject.org/git/" />

    <!-- Remote Poky repo -->
	<project name="poky" remote="yocto-origin" revision="dunfell" >
	</project>

    <!-- Remote meta-raspberrypi repo -->
	<project name="meta-raspberrypi" remote="yocto-origin" revision="dunfell" >
	</project>

    <!-- Remote hLOS repo -->
	<project name="homeLock" remote="github-origin" path="debugger" revision="integration" >
	</project>


</manifest>
                   
